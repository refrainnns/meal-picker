# 小古的专属助手 — 「今天吃什么」开发文档

## 🚀 快速启动（1分钟上手）

```bash
# 进入文件夹
cd ~/Desktop/小古的专属助手

# 启动本地服务器（Python 3）
python3 -m http.server 8080

# 浏览器打开
open http://localhost:8080
```

**手机访问**（同WiFi下）：
1. 查看电脑IP：`ipconfig getifaddr en0`
2. 手机浏览器输入 `http://你的IP:8080`

**手机流量访问**（需电脑保持开机）：
```bash
ssh -R 80:localhost:8080 nokey@localhost.run
# 终端会打印公网URL，如 https://xxxx.lhr.life
```

---

## 项目概述

**「今天吃什么」** 是一款面向中国打工人（9:30-19:30作息）的智能选餐工具，支持三种吃饭方式：
- 🏠 **在家做**：随机菜谱推荐（含食材、步骤、难度、用时）
- 🛵 **点外卖**：基于真实外卖平台的健康/放纵餐推荐（美团/饿了么一键跳转）
- 🍽️ **出去吃**：线下餐厅推荐（火锅/烧烤/粤菜/日料/米其林等）

---

## 技术架构

| 项目 | 说明 |
|:--|:--|
| **类型** | 纯前端单页应用（SPA），单个 HTML + 外部 JS 数据文件 |
| **依赖** | 零框架、零构建工具，纯 HTML/CSS/JS（ES5 兼容） |
| **运行** | 任何 HTTP 服务器即可（Python/Node/Nginx），或用浏览器直接打开 HTML |
| **适配** | iPhone 14 Pro 390px 响应式，safe-area 安全区域，100dvh |
| **数据** | meals-data.js（外卖约150道）+ meals-supplement.js（补充50+道）+ 内联菜谱/餐厅数据 |

---

## 文件结构

```
meal-picker/
├── index.html              # 主页面（所有UI + 业务逻辑）
├── meals-data.js           # 外卖菜品数据库（早餐/午餐/晚餐 x 健康/放纵）
├── meals-supplement.js     # 补充菜品（火锅/咖啡/日料/八大菜系/线下名店）
└── README.md               # 本开发文档
```

---

## 核心功能模块

### 1. 吃饭方式三选一
- **在家做** (`eatMode='home'`)：从 `RECIPES` 对象中随机选菜谱
- **点外卖** (`eatMode='delivery'`)：从 `MEALS_DB` + 补充数据中筛选外卖
- **出去吃** (`eatMode='dinein'`)：从 `DINEIN_SPOTS` 中随机选餐厅

### 2. 口味筛选（点外卖/出去吃模式）
- **35个菜系标签**：覆盖八大菜系（川/粤/湘/鲁/闽/徽/苏/浙）+ 日料 + 韩料 + 海鲜 + 火锅 + 烧烤 + 地方菜
- **多标签匹配**：`CUISINE_RULES` 用正则对 `meal.name + meal.platform` 匹配，一道菜可归属多个菜系
- **下拉式选择器**：节省手机屏幕空间

### 3. 人均预算筛选
- 6档：`¥15-25 / ¥25-40 / ¥40-60 / ¥60-80 / ¥80-120 / ¥120+`
- `PRICE_RULES` 用餐厅/菜品名关键词自动标价

### 4. 健康/放纵餐切换
- 独立开关，与菜系/价格筛选叠加

### 5. 三餐切换（早餐/午餐/晚餐）
- 自动检测当前时间高亮对应餐别

### 6. 定位系统
- IP定位（ipapi.co → ipinfo.io 双API fallback）
- GPS精确定位
- 手动输入城市名
- 逆地理编码（Nominatim API）

### 7. 附近餐厅推荐
- 基于当前餐别+模式的模拟推荐
- 点击跳转高德地图搜索

### 8. 滚动随机动画
- 点击按钮后快速切换12次，模拟老虎机效果

### 9. 历史记录
- 记录最近8次选择，按餐别和放纵标签显示

---

## 数据结构

### 外卖菜品 (MEALS_DB)
```js
{
  name: "老乡鸡·香菇滑鸡套餐+杂粮饭+烫青菜",
  cal: "~480 kcal",
  platform: "老乡鸡/大米先生等中式快餐",
  macros: {
    carb: "杂粮饭100g（低GI）",
    protein: "去皮鸡腿肉120g（约25g蛋白质）",
    fat: "少量烹调用油",
    fiber: "香菇+青菜",
    tip: "备注「米饭减半多菜」「少油少盐」"
  },
  knowledge: "饮食知识文案...",
  source: "——《中国居民膳食指南》"
}
```

### 菜谱 (RECIPES)
```js
{
  name: "番茄鸡蛋面",
  cal: "~380 kcal",
  diff: "简单",           // 难度：超简单/简单/普通/复杂
  time: "15分钟",
  ingredients: "挂面100g、鸡蛋2个、番茄1个...",
  steps: ["番茄切块...", "热锅...", "下面条..."],
  tip: "番茄炒出汁再加水汤更浓"
}
```

### 餐厅 (DINEIN_SPOTS)
```js
{
  name: "海底捞",
  type: "火锅",
  price: "¥80-150",
  dish: "番茄锅+虾滑+捞面",
  vibe: "服务至上的火锅",
  why: "一个人吃也不尴尬，服务员会放娃娃陪你"
}
```

### 菜系匹配规则 (CUISINE_RULES)
```js
{ re:/川|麻辣香锅|水煮|宫保|鱼香/, tags:['川菜'] },
{ re:/湘|湖南|剁椒|辣椒炒肉|费大厨|长沙/, tags:['湘菜'] },
// ... 35+ 规则，用正则匹配菜品名+平台名
```

### 价格匹配规则 (PRICE_RULES)
```js
{ re:/全家桶|九宫格|姜虎东.*烧酒/, price:'¥120+' },
{ re:/豪华|全餐|大董|海底捞|麻六记/, price:'¥80-120' },
// ... 6档规则
```

---

## 如何部署

### 本地运行
```bash
cd meal-picker
python3 -m http.server 8080
# 访问 http://localhost:8080
```

### 公网访问（手机流量可用）
```bash
ssh -R 80:localhost:8080 nokey@localhost.run
# 终端会打印公网 URL 如 https://xxxx.lhr.life
```

### 免费静态托管
可将文件上传至 GitHub Pages / Vercel / Netlify 获得永久链接。

---

## 扩展指南

### 添加新菜品
在 `meals-data.js` 或 `meals-supplement.js` 中按格式追加：
```js
{ name:"店名·菜品名", cal:"~xxx kcal", platform:"外卖平台",
  macros:{carb:"...",protein:"...",fat:"...",fiber:"..."},
  tip:"点餐技巧", knowledge:"饮食知识", source:"来源" }
```

### 添加新菜谱
在 `index.html` 的 `RECIPES` 对象中按格式追加。

### 添加新餐厅
在 `index.html` 的 `DINEIN_SPOTS` 对象中按格式追加。

### 添加新菜系标签
1. 在 `ALL_CUISINES` 数组中追加标签名
2. 在 `CUISINE_RULES` 中追加匹配规则

---

## 关键变量说明

| 变量 | 说明 |
|:--|:--|
| `eatMode` | 吃饭方式：'home'/'delivery'/'dinein' |
| `currentTab` | 餐别：'breakfast'/'lunch'/'dinner' |
| `isHealthy` / `isCheat` | 健康/放纵开关 |
| `selectedCuisines` | 选中的菜系标签数组 |
| `selectedPrice` | 选中的人均预算 |
| `MEALS_DB` | 外卖菜品数据库 |
| `RECIPES` | 菜谱数据库 |
| `DINEIN_SPOTS` | 餐厅数据库 |
| `CUISINE_RULES` | 菜系匹配规则 |
| `PRICE_RULES` | 价格匹配规则 |

---

## 技术栈

- HTML5 + CSS3（CSS Variables / Flexbox / Grid / Safe Area）
- Vanilla JavaScript（ES5 兼容，无任何框架依赖）
- 外部 API：OpenStreetMap Nominatim（逆地理编码）、ipapi.co + ipinfo.io（IP定位）

---

*version 2.0 · 2026年5月*
